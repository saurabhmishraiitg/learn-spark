package org.nexus.scala.spark.general

case object WordCount {
//  val textFile = sc.textFile("hdfs://...")
//  val counts = textFile.flatMap(line => line.split(" "))
//    .map(word => (word, 1))
//    .reduceByKey(_ + _)
//  counts.saveAsTextFile("hdfs://...")
}
